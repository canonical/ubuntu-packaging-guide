.. include:: install.bash
    :code: bash

The following script configues :manpage:`quilt(1)` to look for patches in the
``debian/patches/`` directory if quilt is invoked within a
:term:`source package <Source Package>` directory:

.. include:: /reuse/snippets/quilt/configure.bash
    :code: bash
